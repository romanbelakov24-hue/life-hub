# зависимости
/node_modules
/.pnp
.pnp.*

# сборка next
/.next/
/out/
/build
next-env.d.ts

# локальная база данных (dev-режим без Turso)
*.db
*.db-journal
*.db-wal
local.db*

# переменные окружения
.env
.env.local
.env*.local

# логи и системные файлы
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem
.vercel
*.tsbuildinfo

# мобильная обёртка: артефакты сборки
# сам проект android/ версионируется — в нём лежит нативная конфигурация,
# которую Capacitor не восстановит из capacitor.config.ts
/android/app/build
/android/build
/android/.gradle
/android/local.properties
/android/app/src/main/assets/public
/android/app/src/main/assets/capacitor.config.json
/android/app/src/main/res/xml/config.xml
/ios/App/Pods
/ios/App/build

# ключи подписи и пароли — не версионируются никогда
/android/keystore.properties
*.jks
*.keystore

# воркер pdf.js копируется скриптом перед сборкой
/public/pdf.worker.min.mjs
