import type { Metadata } from "next";

/**
 * Политика конфиденциальности.
 *
 * Нужна для публикации в RuStore: магазин требует действующую публичную ссылку
 * у любого приложения, которое работает с данными пользователя.
 *
 * Лежит вне группы `(app)` — открывается без навигации приложения, потому что
 * ссылку на неё видят посторонние (модераторы магазина, карточка приложения).
 */

export const metadata: Metadata = {
  title: "Политика конфиденциальности",
  description: "Какие данные обрабатывает life hub и как они хранятся",
};

const UPDATED = "4 сентября 2026";

export default function PrivacyPage() {
  return (
    <main className="relative z-10 mx-auto w-full max-w-[680px] px-5 py-12 sm:py-16">
      <p className="eyebrow">life hub</p>

      <h1 className="animate-rise mt-3 font-display text-[30px] font-bold leading-tight tracking-[-0.03em] text-ink sm:text-[40px]">
        Политика конфиденциальности
      </h1>

      <p className="mt-3 text-[13px] text-ink-faint">Обновлено: {UPDATED}</p>

      <div className="mt-8 flex flex-col gap-7 text-[15px] leading-relaxed text-ink-muted">
        <Section title="Коротко">
          <p>
            life hub — личное приложение для учёта расходов и учёбы. Оно не
            собирает данные скрытно, не показывает рекламу, не содержит систем
            аналитики и не передаёт ничего третьим лицам.
          </p>
        </Section>

        <Section title="Какие данные обрабатываются">
          <p>Только то, что вы вводите сами:</p>
          <ul className="mt-3 flex list-disc flex-col gap-1.5 pl-5">
            <li>траты: дата, сумма, категория и ваша заметка к покупке;</li>
            <li>расписание занятий: предмет, аудитория, преподаватель, время;</li>
            <li>задачи и заметки, которые вы создаёте.</li>
          </ul>
          <p className="mt-3">
            Приложение не запрашивает доступ к контактам, геопозиции, камере,
            микрофону, звонкам и сообщениям. В мобильной версии из разрешений
            запрашивается только доступ в интернет.
          </p>
        </Section>

        <Section title="Данные из банковской выписки">
          <p>
            Если вы пользуетесь импортом выписки, файл разбирается прямо в
            браузере на вашем устройстве. Сам файл никуда не загружается: на
            сервер уходят только те операции, которые вы подтвердили в
            предпросмотре, — дата, сумма, описание и выбранная категория.
            Реквизиты карт и счетов не сохраняются.
          </p>
        </Section>

        <Section title="Где хранятся данные">
          <p>
            Записи хранятся в базе данных сервиса Turso (libSQL). Доступ к базе
            есть только у владельца приложения. Резервных копий у третьих лиц не
            создаётся, данные не продаются и не передаются для обучения моделей.
          </p>
        </Section>

        <Section title="Мобильное приложение">
          <p>
            Приложение отображает содержимое сайта и не хранит ваши записи на
            устройстве отдельно от него. Технические файлы веб-браузера
            (например, выбранная тема оформления) остаются локально и удаляются
            вместе с приложением.
          </p>
        </Section>

        <Section title="Публичная сводка">
          <p>
            Владелец может включить страницу со сводкой расходов и поделиться
            ссылкой. Такая страница доступна каждому, у кого есть ссылка, и
            закрывается в один клик в настройках. По умолчанию она выключена.
          </p>
        </Section>

        <Section title="Удаление данных">
          <p>
            Любую запись можно удалить в приложении. Для полного удаления всех
            данных достаточно обратиться к владельцу приложения — база очищается
            целиком, без остаточных копий.
          </p>
        </Section>

        <Section title="Изменения">
          <p>
            Если политика изменится, дата обновления вверху страницы станет
            другой. Действующая редакция всегда доступна по этому адресу.
          </p>
        </Section>
      </div>
    </main>
  );
}

function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <section>
      <h2 className="mb-2 text-[17px] font-semibold text-ink">{title}</h2>
      {children}
    </section>
  );
}
